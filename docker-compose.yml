services:
  db:
    image: postgres:17
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: bookmyticket
    ports: 
      - 5432:5432
    volumes:
      - ./db:/var/lib/postgresql/data
    
volumes:
  pgdata: 

    